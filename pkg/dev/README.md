# pkg/dev/ — fichiers de référence (hors build)

Ce dossier est **exclu du paquet** via `.Rbuildignore` (`^dev$`). Il ne contient
pas de code du paquet, seulement des aides au mainteneur.

## Publication r-universe

Le `packages.json` à placer à la racine du repo **séparé**
`cderv/cderv.r-universe.dev` (PAS dans ce repo-ci) est inliné dans
`PUBLICATION-r-universe.md` — voir ce guide pour les étapes complètes.
