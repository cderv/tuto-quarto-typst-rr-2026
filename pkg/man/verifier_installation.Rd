% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifier-installation.R
\name{verifier_installation}
\alias{verifier_installation}
\title{Vérifier que votre environnement est prêt pour le tutoriel}
\usage{
verifier_installation(tester_rendu = TRUE)
}
\arguments{
\item{tester_rendu}{Logique. Effectuer le rendu de test Typst ? Activé par
défaut. Mettez \code{FALSE} pour un contrôle rapide sans rendu.}
}
\value{
Invisiblement, \code{TRUE} si tout est prêt, \code{FALSE} sinon.
}
\description{
Contrôle, dans l'ordre, votre version de R, la présence et la version de
Quarto, les paquets R prérequis, puis (par défaut) effectue un \strong{rendu de
test} d'un mini-document Typst pour valider la chaîne complète
R -> Quarto -> Typst -> \code{gt} -> \code{ggplot2}. Affiche un bilan et la prochaine
étape conseillée.
}
\details{
Le rendu de test utilise un fichier \code{format: typst} volontairement minimal
(sans \verb{_brand.yml} ni polices) : il valide la chaîne de compilation, mais
pas la chaîne des polices de marque (le vrai point d'attention de
l'exercice 2). Il fonctionne hors-ligne.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
verifier_installation()
verifier_installation(tester_rendu = FALSE)
\dontshow{\}) # examplesIf}
}
