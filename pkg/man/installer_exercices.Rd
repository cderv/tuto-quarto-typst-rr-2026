% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/installer-exercices.R
\name{installer_exercices}
\alias{installer_exercices}
\title{Installer les exercices du tutoriel}
\usage{
installer_exercices(
  dest = "exercices-typst",
  quels = c("tous", "01", "02"),
  force = FALSE
)
}
\arguments{
\item{dest}{Chemin du dossier de destination (créé si besoin). Par défaut
\code{"exercices-typst"} dans le répertoire de travail courant.}

\item{quels}{Quels exercices installer : \code{"tous"} (défaut), \code{"01"}
(document Typst) ou \code{"02"} (projet livre).}

\item{force}{Logique. Passer la confirmation interactive \strong{et} écraser un
dossier de destination déjà existant et non vide ? Par défaut \code{FALSE} (en
mode non-interactif, \code{force = TRUE} est requis pour confirmer la copie).}
}
\value{
Invisiblement, le chemin absolu du dossier de destination, ou \code{NULL}
si l'installation a été annulée.
}
\description{
Copie les fichiers de départ (« starters ») des exercices, embarqués dans le
paquet, vers un dossier de travail local. Seuls les \verb{starter/} sont copiés :
les corrections restent en ligne sur le site du tutoriel.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
installer_exercices()
installer_exercices(quels = "01")
\dontshow{\}) # examplesIf}
}
