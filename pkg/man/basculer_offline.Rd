% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand.R
\name{basculer_offline}
\alias{basculer_offline}
\title{Basculer un exercice en mode hors-ligne (polices Inter locales)}
\usage{
basculer_offline(projet = ".", retour = FALSE)
}
\arguments{
\item{projet}{Dossier de l'exercice (contenant \verb{_brand.yml}). Par défaut le
répertoire courant.}

\item{retour}{Logique. Restaurer le \verb{_brand.yml} d'origine (revenir en
ligne) ? Par défaut \code{FALSE}.}
}
\value{
Invisiblement, le chemin du \verb{_brand.yml}, ou \code{NULL} si rien n'a été
modifié.
}
\description{
Quand le réseau manque, Inter (déclarée \code{source: google} dans \verb{_brand.yml})
ne peut pas être téléchargée. Cette fonction dépose les fichiers Inter
embarqués dans \verb{_fonts/} et bascule l'entrée Inter de \verb{_brand.yml} en
\code{source: file} — \strong{sans toucher} au reste de votre charte (couleurs, etc.).
Votre \verb{_brand.yml} est sauvegardé avant modification.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
basculer_offline()
basculer_offline(retour = TRUE)
\dontshow{\}) # examplesIf}
}
